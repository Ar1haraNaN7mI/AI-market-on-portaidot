{
  "name": "portalproof-market",
  "version": "0.1.0",
  "private": true,
  "description": "Trusted digital delivery marketplace MVP for Portaldot.",
  "scripts": {
    "start": "node server.js",
    "setup": "node scripts/ensure-env.js",
    "demo": "node scripts/start-demo.js",
    "check:js": "node --check app.js && node --check portalproof-contract.js && node --check server.js && node --check scripts/start-demo.js && node --check scripts/ensure-env.js",
    "check:python": "python -m py_compile scripts/portaldot-sdk-service.py",
    "test:smoke": "node scripts/smoke-test.js",
    "test:contract": "cargo test --manifest-path contracts/portalproof_escrow/Cargo.toml",
    "test": "npm run check:js && npm run check:python && npm run test:smoke && npm run test:contract"
  }
}
